class Logger {
public:
    void logMessage(string message) { }
};

class Employee {
public:
    void performTask(Logger logger) {
        logger.logMessage("Task started");
    }
};

int main() {
    Logger officeLogger;
    Employee alice;
    alice.performTask(officeLogger);
    return 0;
}
